- name: install nginx start nginx
  hosts: frontend
  become: yes

  task:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx
    ansible.builtin.package:
      name: nginx
      state: started 
      enabled: yes